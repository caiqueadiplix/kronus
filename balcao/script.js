const data={Lanches:[['X-Burger',18.9],['X-Salada',21.5],['Misto quente',12]],Sucos:[['Suco de laranja',9],['Suco de acerola',8],['Suco de maracujá',10]],Bebidas:[['Coca-Cola lata',6],['Água mineral',4],['Guaraná lata',6]]};
const modal=document.getElementById('modal'),products=document.getElementById('products'),items=document.getElementById('orderItems'),empty=document.getElementById('emptyOrder');let order=[];
const money=v=>v.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
document.querySelectorAll('.category').forEach(btn=>btn.onclick=()=>{const cat=btn.dataset.cat;document.getElementById('modalTitle').textContent=cat;products.innerHTML=data[cat].map(([n,p])=>`<button class="product" data-name="${n}" data-price="${p}"><b>${n}</b><span>${money(p)}</span></button>`).join('');modal.classList.remove('hidden')});
products.onclick=e=>{const p=e.target.closest('.product');if(!p)return;order.push({name:p.dataset.name,price:+p.dataset.price});render();modal.classList.add('hidden')};
function render(){empty.style.display=order.length?'none':'grid';items.classList.toggle('show',!!order.length);items.innerHTML=order.map((x,i)=>`<div class="order-item"><span>${x.name}</span><b>${money(x.price)}</b></div>`).join('');const total=order.reduce((a,b)=>a+b.price,0);document.getElementById('subtotal').textContent=money(total);document.getElementById('total').textContent=money(total)}
document.getElementById('closeModal').onclick=()=>modal.classList.add('hidden');modal.onclick=e=>{if(e.target===modal)modal.classList.add('hidden')};document.getElementById('generate').onclick=()=>{if(!order.length)return alert('Adicione pelo menos um item ao pedido.');alert('Pedido gerado com sucesso!');order=[];render()};
